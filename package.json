{
  "private": true,
  "scripts": {
    "build": "nitro build",
    "dev": "npx prisma migrate dev --name init & nitro dev",
    "prepare": "nitro prepare",
    "preview": "node .output/server/index.mjs"
  },
  "devDependencies": {
    "better-sqlite3": "^9.6.0",
    "nitropack": "latest",
    "prisma": "^6.0.1"
  },
  "dependencies": {
    "@prisma/client": "^6.0.1",
    "@types/jsonwebtoken": "^9.0.7",
    "bcrypt": "^5.1.1",
    "ioredis": "^5.4.1",
    "jsonwebtoken": "^9.0.2"
  }
}
